import 'package:flutter/material.dart';

class Result extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'You did all the test!!!',
        style: TextStyle(fontSize: 20, color: Colors.green),
      ),
    );
  }
}
